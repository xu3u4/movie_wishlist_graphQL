import React from 'react';

import SearchBarContainer from 'containers/searchbar_container';
import WishlistContainer from 'containers/wishlist_container';

const Main = () => (
  <div>
    <SearchBarContainer />
    <WishlistContainer />
  </div>
);

export default Main;
